#include <bits/stdc++.h>

using namespace std;

class Solution {
public:
    int strStr(string haystack, string needle) {
        // definitely not cheating
        return haystack.find(needle);
    }
};
